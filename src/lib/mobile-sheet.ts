/**
 * Share of the map area the phone detail sheet may cover (0–1). The map uses
 * it to frame the selected node in the strip that stays visible above the sheet.
 */
export const MOBILE_SHEET_MAX = 0.58;
